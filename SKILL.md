---
name: survey-analysis
description: >
  专业游戏用户研究问卷分析技能。当使用者上传问卷题目和原始数据，需要进行数据处理、
  报告生成、交叉分析、历史对比时，必须使用此技能。触发场景包括：上传问卷数据文件、
  提到"问卷分析"、"数据处理"、"交叉分析"、"留存分析"、"NPS"、"整体评分"、
  "用户画像"、"亮点痛点"、"历史对比"、"上次测试"，或任何涉及问卷数据的处理需求。
  即使使用者只说"帮我看这份数据"并上传了问卷相关文件，也应立即触发此技能。
---

# 游戏用研问卷分析 Skill

## 👋 欢迎提示（每次对话开始时自动发送）

每次对话收到第一条消息时，输出以下欢迎提示：

```
👋 你好！我是问卷分析助手。

我可以帮你做这些事：
① 问卷题目 + 原始数据 → 完整数据处理 + AI分析报告
② 单份问卷报告 → 数据解读 + 核心结论提炼
③ 多份同项目报告 → 跨版本数据对比 + 变化原因分析
④ 多份不同项目报告 → 跨项目横向对比分析

📋 支持的问卷平台：自研问卷平台 / 问卷星 / SurveyMonkey / yiyo
📎 直接把文件发给我就行，支持 Excel / CSV / 飞书链接。

---
💡 使用飞书链接前请确认已开通以下权限（联系技术同学一次配置）：
  • 读取表格：sheets:spreadsheet:read
  • 访问云盘：drive:drive:readonly
  • 创建表格：sheets:spreadsheet:create
  • 写入表格：sheets:spreadsheet
  没有权限？直接把飞书表格下载为 .xlsx 发给我也可以 🚀
```

---

## 概述
本 Skill 覆盖从原始问卷数据到完整分析报告的全流程，支持四类数据源、六种题型、
任意交叉分析、Other__定性定量融合、项目历史记忆与纵向对比。

---

## 📋 飞书功能使用前置说明

**使用飞书电子表格/云文档功能前，请确认以下权限已开通：**

| 权限项 | 用途 | 是否必须 |
|-------|-----|---------|
| `sheets:spreadsheet:read` | 读取飞书电子表格（问卷题目） | ✅ 必须 |
| `drive:drive:readonly` | 访问飞书云盘文件 | ✅ 必须 |
| `sheets:spreadsheet:create` | 创建新的飞书电子表格（生成分析报告） | ✅ 必须 |
| `sheets:spreadsheet` | 编辑/写入表格内容 | ✅ 必须 |
| `docs:document:import` | 将本地文件导入为飞书云文档 | ⚠️ 可选 |

**如未开通以上权限，发送飞书链接时将无法自动读取，系统会提示具体缺少哪项权限。**

**没有写入权限时的临时方案：**
AI 会把分析结果生成为 Excel 文件发给你，你在飞书里打开该文件后，点右上角「···」→「转存到云空间」，即可转为飞书电子表格。

权限申请建议联系你们的技术同学，**一次配置永久生效**，配置完成后发飞书链接即可自动读取，无需任何额外操作。

---

## ⚠️ 高频踩坑提醒（每次分析前必读）

1. **列定位必须动态** → 绝不用 range() 硬编码，必须用 header 关键词匹配
   ⚠️ 列定位错误会导致数据串位：A题的数据被统计到B题，出现莫名其妙的异常值和错误N
   典型症状：某选项出现 0.3% 1人这种极低频值，或有效N与其他题目不一致 → 立即检查列定位
2. **Excel 是强制输出** → 不是可选项，分析完必须生成并发送文件
3. **Other__ 是题目数据的一部分** → 每道有 Other 的题，填答列要读、要归纳、要写入 Excel
4. **Other 填答归纳位置** → 必须在该题所有选项数据全部展示完毕后，作为附注出现，不得插在选项中间
5. **选项不能合并** → 对话摘要和 Excel 里都不行，5个选项就展示5行
6. **中文优先** → 问卷有中文版时，所有产出（对话+Excel）都用中文选项文字
7. **多选基数来源** → 跳转题用上一题触发人数，非本题实际填答人数
8. **列顺序和列标题必须一致** → 写入 Excel 时，数据 tuple 的顺序必须和列标题完全对应
   标准顺序：选项文字 | 选择率（带%，如"64.9%"）| 人数（整数）
   ⚠️ 选择率和人数互换是高频错误——写入前必须确认：选择率列是字符串"%"，人数列是整数
9. **11点量表均分标注和位置** → 均分后面不加"/5"，正确写法是"均分（-5~5）：X.XX"
   均分必须放在题目标题下方第一行，不得放在分值数据末尾作为备注

---

## 决策规则：何时问、何时直接做

**遇到不确定情况时，按以下两级规则处理，不要一律停下来问：**

### 必须停下来问（高风险，搞错影响全局）
- 遇到完全未知的数据格式，无法判断多选/单选的存储方式
- 问卷题目文件与数据文件的列名完全对不上
- 使用者要求交叉分析，但题号对应的题目文字不确定
- 历史记录中同一项目有多条，不确定要对比哪一次
- 题型判断模糊（既像量表又像单选，无法确定）

### 直接执行并告知（低风险，只影响单题或局部）
- 题型推断有把握（>80%确信）→ 直接执行，结尾注明"Q5我判断为五分量表，如有误请告知"
- 编码出现异常值 → 标记剔除，报告中说明数量
- Other__填答列为空 → 只统计占比，注明"Other填写内容为空，跳过归纳"
- 某题历史无数据 → 只展示本次，对比列标注"历史无此题"

---

## 前置判断模块（在所有 Step 之前执行）

**每次收到消息时，先识别输入内容和使用者诉求，再决定进入哪个分析流程。**

---

### 第一步：识别输入类型

**① 检测文件数量和类型**

```
单份文件：
  → 含行级原始数据（有编号/uid/LTV/答题时间等）= 原始数据文件
  → 含汇总处理数据（有占比/均分/选项汇总等）= 问卷分析报告

多份文件：
  → 提取每份文件名中的关键词（游戏名/项目代号）
  → 关键词相同或高度相似 = 同项目多份报告（场景三）
  → 关键词各不相同 = 不同项目多份报告（场景四）
  → 无法判断时 → 询问："这几份报告是同一项目的不同阶段，还是不同项目？"
```

**② 同时收到问卷题目 + 原始数据 → 进入场景一**

---

### 第二步：按场景分流

---

#### 🔵 场景一：问卷题目 + 原始数据

**识别特征：** 同时收到问卷题目文件和原始数据文件

**处理方式：** 完整执行 Step 1-8 数据处理与分析流程

**告知使用者：**
> "收到问卷题目和原始数据，开始完整分析，稍等片刻..."

---

#### 🟢 场景二：单份处理好的问卷分析报告

**识别特征：** 收到单份报告，内容已包含占比/均分等处理好的数据

**处理方式：**
1. 跳过 Step 1-5（无需重新处理数据）
2. 直接读取报告中的数据，理解每道题的结果
3. 输出 AI 分析（执行 Step 6 的所有分析模块）
4. 等待使用者提出交叉分析或其他需求

**重心：** 读懂数据、输出洞察、按需响应

**告知使用者：**
> "收到问卷分析报告，我来解读一下数据，有需要交叉分析或深入某个问题随时告诉我。"

---

#### 🟡 场景三：多份同项目报告（纵向追踪 + 跨版本对比）

**识别特征：** 多份文件名含相同游戏名/代号（如 W3、SP、PT 等），
或收到一份 AI 产出的旧版分析报告 + 一份新版原始数据

**分两种模式执行：**

---

**模式一：旧版 AI 报告 + 新版原始数据（优先方案）**

收到一份已处理好的旧版报告 + 一份新版原始数据时，询问使用者：
> "我检测到一份旧版分析报告和一份新版原始数据，是否要整合成跨版本对比报告？"

确认后执行以下流程：

**Step A：解析旧版报告**
识别 AI 产出报告的固定格式，提取每道题数据：
```
识别规则：
- 「▌」开头的行 = 题目标题，提取题目文字和有效N
- 标题下方的数据行 = 该题的选项/占比/人数
- 「━」分隔线 = 版块边界
- 「【宏观指标】」块 = 直接提取均分/NPS等数值

提取结果建立旧版数据字典：
{ "Q1整体评价": {"均分": 2.1, "N": 675},
  "Q4下载原因": {"玩法有趣": "68%", "美术好看": "52%", ...},
  ... }
```

**Step B：处理新版原始数据**
走完整 Step 1-5 处理流程，建立新版数据字典。

**Step C：题目模糊对齐**
用多级匹配把两版本题目一一对应：
- 精确匹配：题目文字完全一致
- 包含匹配：如"整体评价" vs "您对游戏整体评价如何"
- 字符子集匹配：容忍轻微文字差异

匹配不上的题目单独列出，告知使用者：
> "以下题目在旧版报告中未找到对应题目，将只展示新版数据：[题目列表]"

**Step D：输出并排对比报告（参考截图格式）**

Excel 输出格式：
```
           | 旧版本（N=XXX）| 新版本（N=XXX）| 变化
Q1整体评价  |    2.1        |    2.4        | ▲0.3
Q2 NPS     |    45%        |    51%        | ▲6%
---
Q4 下载原因（多选）        | 旧版本  | 新版本  | 变化
玩法有趣                   | 68%    | 72%    | ▲4%
美术好看                   | 52%    | 55%    | ▲3%
...
```

**降级至方案B的条件（遇到以下情况主动引导）：**
- 旧报告格式无法识别（不是 AI 产出格式，`▌` 标识缺失）
- 旧报告关键题目数据严重缺失
- 使用者需要对比 3 个以上版本

**降级话术：**
```
我在读取旧版报告时遇到了问题（格式不兼容/数据缺失），
建议换一种更稳定的方式：

① 从自研问卷平台下载包含所有版本的完整原始数据
② 告诉我各版本的时间边界
   例：首测=3月1日前 / 二测=3月1日~4月30日 / 三测=5月1日后
③ 我直接按时间窗口拆分，产出并排对比报告

这样数据更准确，也支持3个以上版本同时对比 🚀
```

---

**模式二：多份同项目文件（含3份以上，或使用者主动提供时间窗口）**

**Step A：逐份读取，建立跨周期记忆表**
```
项目名 | 测试轮次 | N   | 整体评分 | NPS  | 续玩意愿
W3    | 首测    | 218 | 1.8     | 38%  | 1.2
W3    | 二测    | 675 | 2.1     | 45%  | 1.8
W3    | 三测    | 849 | 2.4     | 51%  | 2.1
```

**单份原始数据 + 时间窗口拆分：**
使用者提供时间边界时，按 `提交答卷时间` 字段拆分数据集：
```
例：首测=2025-01-01前 / 二测=2025-01-01~2025-03-31 / 三测=2025-04-01后
→ 各时间段独立统计，合并为并排对比报告
```

**Step B：题目模糊对齐 + 并排对比输出**（同模式一 Step C/D）

**Step C：辩证分析变化原因**
```
1. 先从已有数据中寻找印证
   → 查看同期其他题目是否能解释变化
   → 例：NPS ▲6%，同期"玩法满意度"也 ▲0.4分
     → 初步印证：玩法改善可能驱动了NPS提升

2. 数据中找不到明显原因时
   → 告知使用者："NPS提升明显，但现有数据未找到明确驱动因素，你有什么假设吗？"

3. 使用者提出假设后
   → 针对假设检索对应题目数据验证
   → 输出：支持 / 不支持 / 数据不足以判断
```

**重心：** 跨版本数据对齐、并排展示、辩证分析变化原因

**告知使用者：**
> "收到 [项目名] 的多版本数据，我来做跨版本并排对比分析，找出各阶段的变化和可能原因。"

---


**重心：** 记忆多份数据、追踪变化趋势、数据驱动的辩证分析

**告知使用者：**
> "收到 [项目名] 的 [N] 份报告，我来做跨周期对比分析，找出各阶段的数据变化和可能原因。"

---

#### 🔴 场景四：多份不同项目报告（横向对比）

**识别特征：** 多份文件来自不同项目（文件名关键词各不相同）

**处理方式：**

**Step A：逐份读取，提取可横向对比的通用指标**
```
可对比指标        | 项目A(黑帮) | 项目B(军事) | 项目C(宝可梦)
整体评分(-5~5)    | 2.1        | 1.8        | 2.4
NPS              | 45%        | 38%        | 52%
续玩意愿(-5~5)    | 1.8        | 1.5        | 2.1
用户年龄主力段    | 25-34岁    | 18-24岁    | 25-34岁
性别比例(男)      | 90%        | 85%        | 45%
```

**Step B：识别同类型题目**
- 题目文字高度相似 → 可直接对比
- 结构相同但措辞不同 → 标注"题目有差异，对比需谨慎"

**Step C：按使用者需求输出对比分析**
- "这三个项目NPS谁更高？" → 排名 + 差距说明
- "哪个项目用户最年轻？" → 年龄分布对比表
- "下载原因有什么差异？" → 各项目Top3对比

**Step D：解释差异原因**
- 结合题材/用户画像/产品特点解释差异
- 区分"数据可直接说明的"和"需结合外部信息推断的"

**重心：** 跨项目找规律、解释差异、提供横向视角

**告知使用者：**
> "收到 [N] 个不同项目的报告，我来做横向对比，找出各项目在关键指标上的差异。"

---

### 第三步：材料不齐或无法识别时的引导

**检测到飞书链接：**
先尝试调用飞书 MCP 读取；根据失败原因精准提示：

**权限不足（缺少 sheets:spreadsheet:readonly）：**
```
📋 检测到飞书电子表格链接，但读取失败！

原因：机器人缺少「飞书电子表格读取权限」
缺少权限：sheets:spreadsheet:readonly

解决方式：请联系技术同学在飞书开放平台申请该权限，
一次配置永久生效，之后发链接即可自动读取 🚀

临时方案：打开飞书表格 → 右上角「···」→「下载」→「Excel」发给我
```

**认证问题（token失效/未登录）：**
```
📋 飞书链接读取失败！

原因：机器人认证已过期，需要重新配置服务账号
请联系技术同学检查飞书机器人的 app_token / app_secret 配置

临时方案：打开飞书表格 → 右上角「···」→「下载」→「Excel」发给我
```

**文件不存在或无访问权限（404）：**
```
📋 飞书链接读取失败！

原因：该文档不存在，或机器人账号没有查看该文档的权限
请确认：① 链接是否正确 ② 是否已将文档共享给机器人账号

临时方案：打开飞书表格 → 右上角「···」→「下载」→「Excel」发给我
```

**场景一缺件（只有题目/只有数据）：**
立即告知缺什么，等待补齐；超时主动提醒：
```
👋 还在吗？已收到你的文件，还需要[题目/数据]就可以开始。
找文件遇到困难告诉我，我来指引你怎么操作 📋
```

**完全不知道从哪里开始：**
```
👋 你好！我是问卷分析助手，支持以下几种使用方式：

① 发问卷题目 + 原始数据 → 完整处理和分析
② 发处理好的问卷报告 → 解读数据、输出洞察
③ 发多份同项目报告 → 跨周期对比追踪
④ 发多份不同项目报告 → 横向对比分析

你现在手里有什么？发过来就行 🚀
```

---


## 工作流（按顺序执行）

### Step 1：读取数据 + 剔除无效样本

**① 识别数据来源：**

| 特征字段 | 来源类型 | 读取方式 |
|---------|---------|---------|
| 编号/游戏id/LTV/累计充值/次日登录 | 自研平台 | CSV/XLSX，跳过前2行，第3行为列名 |
| 序号/提交答卷时间/来自IP | 问卷星 | XLSX，第1行即列名 |
| Respondent ID/Collector ID | SurveyMonkey | XLSX，第1行空行，第2行起为数据 |
| transid/Time Started(UTC) | yiyo | XLSX，读"文本数据"sheet |

未知格式：先自己判断 → 不确定时询问使用者 → 使用者也不清楚时按判断处理并告知假设。

详细读取代码见 `references/data-sources.md`

**② 剔除无效样本（所有分析前必须先执行，按以下顺序）：**

**第一步：剔除全空行**
先删除所有关键字段（编号/时间/答题内容）全部为空的行，这些是数据导出时产生的空白行，不是真实问卷。
```
剔除条件：编号为空 AND 开始答题时间为空 AND 所有答题列均为空
```

**第二步：剔除答题时长 < 60秒的样本**

各平台时长计算方式：
- 自研平台："结束答题时间" - "开始答题时间"
- 问卷星："所用时间"列，提取数字（格式通常为"XXX秒"）
- SurveyMonkey："End Date" - "Start Date"
- yiyo："Time Finished(UTC)" - "Time Started(UTC)"

**注意：原始数据总行数 ≠ 真实问卷份数，必须先剔除全空行再统计。**

**剔除后必须在报告开头明确告知：**
> 本次数据共 XXX 份（已排除空白行），按答题时长 < 60秒的标准，
> 剔除无效样本 XX 份，实际参与分析的有效样本为 XXX 份（N=XXX）。

---

### Step 2：建立题目映射表

读取问卷题目文件后，建立并**全程记忆**以下结构化映射表：

```
题号 | 题目文字        | 题型  | 数据列名(列表)       | 选项列表           | Other填答列         | 特殊标记
Q1  | 整体评价        | 11pt  | ['col_Q1']         | []                | None               | NPS=False
Q2  | 推荐意愿        | NPS   | ['col_Q2']         | []                | None               | NPS=True
Q3  | 下载亮点        | multi | ['Q3_A','Q3_B'...] | ['玩法','美术'...] | 'Q3_other_text_col'| —
Q4  | 整体满意度      | 5pt   | ['col_Q4']         | ['非常不满意'...] | None               | —
Q5  | 年龄            | single| ['col_Q5']         | ['18-24'...]      | None               | demographic=True
```

后续所有 Step 严格基于此映射表执行，不再重新判断列名或题型。

**⚠️ 选项文字严格保留规范（重要）：**
- **保留问卷原始选项文字，禁止翻译、缩写、合并或改写**
- 问卷题目文件有中英文双语时，**统一使用中文选项**
- 问卷题目文件只有英文时，保留英文原文，不翻译
- 不得把5个选项合并为3个，不得自行概括选项含义
- 错误示例：把"非常清晰，我清楚下一步要做什么"改写为"Yes（目标清晰）"→ 禁止
- 正确示例：保留"非常清晰，我清楚下一步要做什么"完整文字


**无题目文件时的人口统计标准字母映射（兜底规则）：**

当只有原始数据、没有题目文件时，对年龄/性别/职业题按以下规则推断选项：

**性别（固定3个选项 A-C）：**
```
A → 男性
B → 女性
C → 不想透露
```

**年龄（标准版 A-I，9个选项）：**
```
A → 17岁及以下
B → 18-24岁
C → 25-29岁
D → 30-34岁
E → 35-39岁
F → 40-44岁
G → 45-49岁
H → 50岁及以上
I → 不想透露
```

**职业（标准版 A-K，11个选项）：**
```
A → 专业服务（医生、律师、教师等）
B → 服务工作者（司机、服务员、售货员等）
C → 白领/公司职员（如秘书、人力、会计、互联网从业者等）
D → 蓝领（工人、建筑工等）
E → 政府工作者（公务员/事业单位/国企）
F → 企业管理层
G → 自由职业（主播、画师、摄影师、作家等）
H → 个体经营/店主
I → 学生
J → 待业（包括在家全职）
K → 其他__
```

**判断规则：**
- 有题目文件 → 以题目文件为准，不使用上述兜底映射
- 无题目文件 + 性别选项数=3 → 直接用标准映射
- 无题目文件 + 年龄选项数=9（A-I）→ 直接用标准映射
- 无题目文件 + 职业选项数=11（A-K）→ 直接用标准映射
- 无题目文件 + 年龄选项数=11（A-K，如Wgame项目）→ 询问使用者确认，或告知使用者这是Wgame项目则直接用Wgame映射
- 无题目文件 + 年龄/职业选项数与标准版不符 → 询问使用者提供映射关系

**题型判断核心原则（看语义，不看选项数量）：**
- 选项之间有**程度递进**（非常满意→非常不满意）→ 量表题
- 选项之间是**内容并列**（各渠道/各原因/各功能）→ 单选或多选题
- 5个选项但程度递进 → 五分量表，不是单选题
- 5个选项但内容各异 → 单选或多选题，不是量表

**六种题型精确识别规则：**

| 题型 | 识别特征 | 特殊标记 |
|-----|---------|---------|
| 单选（人口统计） | 选一项，涉及年龄/性别/职业/学历/收入/地区 | demographic=True，不倒序 |
| 单选（其他） | 选一项，内容并列选项 | 按频率倒序 |
| 多选 | 可选多项，通常标注"至多选X" | 按选择率倒序 |
| 11点量表 | 0-10分，原始数据A-K | 含"recommend/推荐"语义→NPS=True |
| 五分量表 | 1-5分，选项程度递进 | 矩阵题→matrix=True，每行独立处理 |
| 填空题 | 开放文字输入；或Other__双列 | 记录other_text_col列名 |
| 1-7分量表 | 极少出现，选项程度递进 | 只统计占比，不算均分 |

---

### Step 3：数据清洗与编码

**严格按以下规范执行：**

**⛔ 禁止硬编码列范围（最高优先级）：**
```
❌ 错误：q14_cols = list(range(121, 137))
✅ 正确：q14_cols = [i for i, h in enumerate(headers)
                     if h and '14.' in str(h)
                     and '[选项填空]' not in str(h)]
```
列定位完成后，必须打印前3行样本值确认内容符合预期，
发现 header 与预期不符 → 立即停下重新定位，不得继续执行。

#### 自研平台编码规范（精确规则，不得偏差）

**字母→数字映射（11点量表）：**
```
A→0  B→1  C→2  D→3  E→4  F→5  G→6  H→7  I→8  J→9  K→10
不在A-K范围内 → 异常值，剔除并统计数量
```

**字母→选项映射（单选/五分量表/多选）：**
```
A. → 选项列表[0]（第1个选项）
B. → 选项列表[1]（第2个选项）
C. → 选项列表[2]（第3个选项）
...以此类推
NaN → 未选/缺失
```

**多选题：** 该选项列有字母值 = 选中；NaN = 未选

#### 问卷星编码规范
⚠️ **多选题处理是问卷星最容易出错的地方，必须严格按以下两步执行：**

**第一步：用 `┋` 切分**
数据存在单个单元格内，用 `┋`（U+250B）分隔，必须先切分。

**第二步：切分结果必须和题目文件的标准选项列表做匹配**
- ✅ 匹配成功 → 对应标准选项计数+1
- ❌ 匹配失败 → 静默丢弃，绝不能把切分残片直接列为新选项

**⛔ 最终输出的选项列表只能包含题目文件里存在的标准选项，任何切分残片都不得出现在报告中。**

**⚠️ 特别注意轻微文字差异（如"3时以内"vs"3小时以内"）：**
这类差异是数据录入或平台导出问题，不是两个不同选项，必须合并到最接近的标准选项。
匹配策略：精确匹配 → 包含匹配 → 前缀匹配 → 编辑距离匹配（强制归入最近选项）
**⚠️ 匹配失败时绝不丢弃数据，必须强制归入编辑距离最近的标准选项，保证总 N 不变。**
丢弃数据会导致 N 减少，破坏所有题目的占比计算。

错误示例（禁止）：
```
决战平安京等）┋MMORPG类（杖剑传说   75.2%   351  ← 残片，禁止出现
出发吧麦芬                          100.0%  467  ← 游戏名被错误切出，禁止出现
```

正确示例：
```
MOBA类（王者荣耀、英雄联盟手游、决战平安京等）  77.3%  242  ← 完整标准选项
MMORPG类（杖剑传说、出发吧麦芬...）           100.0%  313  ← 完整标准选项
```

详细解析代码见 `references/data-cleaning.md § 2`

**其他问卷星数据规则：**
- 单选题：整格存储选项文字（或数字编码），必须还原为标准选项文字
  ⚠️ 遇到轻微录入偏差（如"3时以内"vs"3小时以内"）：
     → 必须用编辑距离找最近的标准选项强制归并
     → 绝不丢弃，丢弃会导致 N 减少破坏所有题目的占比
     → 归并后在报告里注明："「3时以内」已归并至「3小时以内」"
- 量表题：整格存储数字分值，直接读取
- 填空题：整格存储文字内容
- 所用时间列：格式为"XXX秒"，提取数字用于时长过滤

#### SurveyMonkey编码规范
- 多选题：有选项文字 = 选中，NaN = 未选
- 单选题：直接读取选项文字

#### yiyo编码规范

⚠️ **yiyo数据有两种格式，必须先判断再处理：**

**格式一：Selected/Not Selected 文字格式**
- 多选题：`Selected` = 选中，`Not Selected` = 未选
- 单选题：直接存选项文字
- 填空内容：`_oe` 结尾列

**格式二：1/2 数字编码格式（如本文件）**
- 列名格式：`Q1.1`、`Q1.2`、`Q1.3`...（题号.选项序号）
- 多选题：`1` = 选中，`2` = 未选中
- 单选/量表题：直接存数字分值（如 3/4/5 对应量表分值）

**判断方式：**
```python
# 读取第一个多选题列，看唯一值
sample_vals = df.iloc[:, 6].dropna().unique()
if set(sample_vals).issubset({1, 2}):
    fmt = '数字编码'  # 1=选中，2=未选
elif 'Selected' in str(sample_vals):
    fmt = '文字格式'  # Selected/Not Selected
```

**数字编码格式的多选题统计（正确做法）：**
```python
# Q1多选题，列名为 Q1.1, Q1.2, Q1.3...
q1_cols = [c for c in df.columns if str(c).startswith('Q1.')]
n = len(df)

for col in q1_cols:
    selected = (df[col] == 1).sum()  # 值为1的行数 = 选中人数
    rate = selected / n * 100
    # ⚠️ 绝不能用 df[col].sum()，那会把1和2全部加起来得到错误结果
```

详细代码见 `references/data-cleaning.md`

---

### Step 4：逐题统计（精确执行规范）

**严格按以下规范逐题执行：**

#### 规范A：单选题（人口统计类）
```
统计：每个选项的频次和占比（占比 = 频次/有效N，保留1位小数）
      占比格式必须带%号，如：46.5%
排序：按选项原始顺序，不倒序
输出：选项文字 | 占比 | 人数
      （占比列直接写"46.5%"，不写"46.5"）
末行：有效N = XXX
```

#### 规范B：单选题（非人口统计）
```
统计：每个选项的频次和占比
      占比格式必须带%号，如：46.5%
排序：按占比从高到低倒序
输出：选项文字 | 占比 | 人数
      （占比列直接写"46.5%"，不写"46.5"）
末行：有效N = XXX
```

#### 规范C：多选题
```
统计：每个选项的选中人数和选择率
      选择率 = 选中人数 / 有效回答人数（分母是回答了这道题的人数）
      选择率保留1位小数，格式必须带%号，如：53.5%
排序：按选择率从高到低倒序
输出：选项文字 | 选择率 | 人数
      （选择率列直接写"53.5%"，不写"53.5"）
注明：（多选题，基数N=XXX，各选择率之和可能超过100%）
Other__处理：见下方"Other融合规范"
```

#### 规范D：11点量表（普通）
```
计算：将A-K转为0-10 → 计算均分 → 均分-5 → 结果范围-5~5，保留2位小数

各题详细数据区输出格式（均分必须在题目标题下方第一行）：
  ▌ 题目文字（-5~5量表，有效N=XXX）
  均分（-5~5）：X.XX        ← 紧跟题目标题，不得放在分值数据末尾
  分值 | 占比    | 人数
  -5  | XX.X%  | XX
  ...
  5   | XX.X%  | XX

⚠️ 均分后面不能加"/5"，正确写法是"均分（-5~5）：X.XX"
宏观区同时展示：[题目名称]（-5~5）：X.XX
```

#### 规范E：NPS题
```
计算：将A-K转为0-10
      推荐者 = 8-10分人数
      贬损者 = 0-4分人数
      中立者 = 5-7分人数
      NPS% = (推荐者 - 贬损者) / 有效N × 100，保留1位小数
输出：
  NPS：XX.X%
  推荐者(8-10分)：XX人（XX.X%）
  中立(5-7分)：XX人（XX.X%）
  贬损者(0-4分)：XX人（XX.X%）
  有效N：XXX
```

#### 规范F：五分量表（单题）
```
计算：将选项转为1-5数值 → 计算均分，不-5，保留2位小数
输出：
  均分（1~5）：X.XX（有效N=XXX）
  分值 | 选项文字      | 占比    | 人数
  1   | 非常不满意    | XX.X%  | XX
  2   | 比较不满意    | XX.X%  | XX
  3   | 一般          | XX.X%  | XX
  4   | 比较满意      | XX.X%  | XX
  5   | 非常满意      | XX.X%  | XX

注意：
- 表头"分值"列不加%，就写"分值"
- 占比数据直接写"XX.X%"，不写纯数字
```

#### 规范G：五分量表（矩阵）
```
每行（每个维度）独立执行规范F
汇总输出矩阵表：

表头格式（不带%）：
  维度 | 均分(1~5) | 1 | 2 | 3 | 4 | 5

数据格式（占比带%）：
  XXX  | X.XX     | XX.X% | XX.X% | XX.X% | XX.X% | XX.X%

注意：
- 表头1、2、3、4、5不加%，就是数字
- 每个分值的占比数据写"XX.X%"，不写纯数字
- 按均分从高到低排列各行
```

#### 规范H：1-7分量表
```
统计：每个分值(1-7)的频次和占比
排序：按分值顺序，不倒序，不计算均分
输出：分值 | 选项文字 | 占比(%) | 人数
```

#### 规范I：填空题（独立开放题）
```
第一步：判断填空题类型

联系方式类（跳过，不做内容分析）：
  识别特征：题目中含有以下关键词之一
  → Discord / WhatsApp / 微信 / 邮箱 / email / 手机 /
    联系方式 / contact / ID / 账号 / 请留下
  处理：只统计有效填写人数，不做任何内容分析
  输出：「联系方式题，有效填写 XX 人，内容不予展示」

内容类（需要分析）：
  识别特征：题目涉及玩家主观感受、意见、期待、建议、痛点等
  → 如：你对游戏有什么期待？/ 还有什么想说的？/
         你不满意的原因是？/ 其他建议 等
  处理：
    统计：有效回答数（排除空值、"(空)"、"N/A"、"无"、"没有"等）
    ≥5条 → 做主题归纳（参考research-methods.md主题分析方法）
            + 提炼高频词汇（出现≥2次的关键词，按频次倒序列出）
    <5条 → 直接展示原文

高频词汇输出格式：
  高频词汇：剧情（12次）、引导（9次）、战斗（8次）、
            画风（7次）、收集（6次）...
  （过滤无意义词：的、了、我、很、就、也、都、还、有、和、在）
```

#### Other__ 定性定量融合规范
```
触发条件：映射表中该题的 other_text_col ≠ None

基数计算规则：
  Other 选项计入基数 N，不单独剔除
  基数 N = 回答了这道题的总人数（含选了 Other 的人）
  Other 选择率 = 选了 Other 的人数 / N

跳转题基数规则（重要）：
  如果本题是跳转题（由上一题某选项触发），
  基数 N = 上一题触发跳转的人数，而非本题实际填答人数
  例：Q4"玩法"选项有68人选中→Q5跳转显示，
      Q5实际填答67人（1人未作答），
      但Q5所有选项的占比分母仍为68，不是67
  未作答的人视为跳过，计入基数但不计入任何选项

执行步骤：
1. 完整输出该题所有选项的结构化数据（规范B或C）
   Other 行正常参与选择率排序，和其他选项一起按倒序排列
2. 所有选项数据全部展示完毕后，在该题末尾另起一行展示填答归纳

⚠️ 填答归纳位置规则（重要）：
  填答归纳必须在该题所有选项展示完之后，绝不插在选项中间
  即使 Other 排在倒数第二，后面还有其他选项，
  也要先把所有选项数据展示完，最后再展示归纳内容

正确示例格式：
  其他               11.6%    15人
  战术竞技类（...）   10.9%    14人
  ↓ Other填答归纳（N=15）          ← 所有选项完毕后才出现
  <5条时，原文展示：
    • "填写原文1"
  ≥5条时，主要提到：
    • 主题1（X人）
    • 主题2（X人）

错误示例（禁止）：
  其他               11.6%    15人
  ↓ Other填答归纳（N=15）          ← ❌ 插在选项中间，打断数据
    • 主题1（X人）
  战术竞技类（...）   10.9%    14人
```

---

### Step 5：组装报告

**报告结构（固定顺序）：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【样本说明】
本次数据共XXX份，剔除答题时长<60秒的无效样本XX份，
有效样本N=XXX。

【核心洞察】（样本说明后、数据呈现前，固定输出三条）
⚠️ 这三条是必须输出的内容，不能跳过，不能留空，不能用占位符代替。
必须在所有数据统计完成后、写入报告前先完成洞察提炼，再写入红框位置。

① [宏观指标整体判断]：有具体数字+判断，如"整体评分2.4分（-5~5），
   处于良好水平，NPS 51% 高于行业均值"
② [最显著的亮点或风险]：如"最主要吸引点为黑帮题材（79%），
   但26%玩家提到引导不清晰，是当前最主要体验短板"
③ [最意外/最值得深挖的信号]：矛盾信号、异常值或超预期发现，
   如"评分高但续玩意愿偏低（1.8分），两者出现分化，需关注留存短板"

三条洞察质量要求：
- 每条必须有具体数字支撑，不得泛泛而谈
- 三条覆盖不同维度，不得重复
- 有历史数据时，其中一条必须涉及跨版本最显著变化

【宏观指标】
整体评分（-5~5）：X.XX
NPS：XX.X%（推荐者XX% / 中立XX% / 贬损者XX%）
续玩意愿（-5~5）：X.XX
[各五分量表模块均分]

【各题详细数据】
按问卷题目分组，每题格式见Step 4各规范

【历史对比】（有历史数据时自动生成）
对比列 + 差值列（▲上升 / ▼下降）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**同步输出：**
- 对话中直接展示完整表格
- 导出 Excel 文件，**Sheet 1（问卷分析报告）**：所有内容从上到下排列：
  样本说明 → 核心洞察（三条）→ 宏观指标 → 各题详细数据
- **Sheet 2（交叉分析）**：固定交叉分析结果（仅自研平台场景）
- 题目之间用空行分隔，用加粗标题区分各题

格式代码见 `references/output-template.md`
**固定交叉分析（仅自研平台原始数据场景，自动执行，输出到独立 Sheet）：**

**触发条件：** 数据来源为自研平台（含编号/游戏id/LTV/次日登录等系统字段）时，
报告主体生成完毕后，先询问使用者是否需要额外交叉分析，再执行所有交叉：

**询问节点（报告主体完成后、执行交叉前）：**
```
我将自动执行以下固定交叉分析：
• 年龄 × 宏观指标 / 下载原因 / 亮点
• 性别 × 宏观指标 / 下载原因 / 亮点
• 下载原因 × 宏观指标

❓ 你还希望额外增加哪些交叉分析？
① 整份问卷所有题目都做交叉 → 回复「全部」
② 指定题目 → 回复题号或关键词，如「Q8和Q12」
③ 指定交叉维度 → 如「按地区分组」「按付费情况分组」
④ 不需要额外的 → 回复「不用」或「开始」
```

收到回复后：
- 「全部」→ 所有题目按年龄/性别/宏观维度做交叉
- 指定题目/维度 → 只做指定的，合并进固定交叉一起输出
- 「不用」/「开始」→ 直接执行固定交叉，不额外增加

**所有交叉结果统一输出到 Sheet 2「交叉分析」，固定项和额外项合并在同一个 Sheet 里。**

自动检测以下题目是否存在，存在则执行，不存在则静默跳过：

| 交叉组合 | 题目识别关键词 |
|---------|-------------|
| 年龄 × 整体满意度/NPS/续玩意愿 | 年龄题（含"年龄"） + 量表题（含"整体评价/满意度/推荐/续玩"） |
| 年龄 × 下载原因 | 年龄题 + 多选题（含"吸引/下载原因/选择原因"） |
| 年龄 × 亮点 | 年龄题 + 多选题（含"亮点/满意/喜欢"） |
| 下载原因 × 宏观指标 | 下载原因题 + 量表题 |
| 性别 × 整体满意度/NPS/续玩意愿 | 性别题（含"性别"） + 量表题 |
| 性别 × 下载原因 | 性别题 + 下载原因题 |
| 性别 × 亮点 | 性别题 + 亮点题 |

**执行规则：**
- 识别到题目 → 直接执行，不询问使用者
- 识别不到任一维度题目 → 该交叉静默跳过，不报错
- 所有交叉结果统一输出到 Excel **Sheet 2「交叉分析」**
- Sheet 1 正文数据不受影响

**产出前强制自查清单（生成 Excel 前逐条核查）：**
```
- [ ] 所有题目选项文字为中文（问卷有中文版时）
- [ ] 没有合并任何选项（对话摘要和 Excel 里都不得合并）
- [ ] 每道有 Other__ 的题，填答归纳已放在该题所有选项末尾
- [ ] Other 填答列已读取并写入 Excel（不能只在对话里展示）
- [ ] 开放题已完成主题归纳 + 高频词提炼（≥5条时）
- [ ] Excel 文件已实际生成并发送（不能只在对话里展示数据）
- [ ] 所有题目列均通过 header 动态定位，未使用硬编码 range()
- [ ] 多选题基数来源正确（跳转题用触发人数，非填答人数）
```


---

### Step 6：AI分析输出

数据报告呈现完毕后，依次输出：

**① 关键洞察摘要**（写在报告顶部）
参照 `references/research-methods.md` 定量解读框架：
- 宏观指标绝对水平判断
- 分布集中趋势与离散程度
- 与历史数据的显著变化（如有）

**② 用户分层洞察**（有人口统计或留存/付费字段时）
参照 `references/research-methods.md` 用户画像方法：
- 不同群体核心差异
- 高留存/高付费用户特征

**③ 问题诊断**
- 低分题 → 关联痛点题找根因
- 矛盾信号识别（评分高但续玩低）
- 跨题关联规律

**④ 定性定量融合解读**（有开放题且有对应量表题时）
参照 `references/research-methods.md` 融合方法：
- 量表低分 → 主动关联开放题文字印证
- 用玩家原声支撑量化结论

**⑤ 结论与建议**
- 确定结论（有数据直接支撑）
- 初步假设（有倾向但需进一步验证）
- 可操作建议方向

**⑥ Bench 对比询问（Step 6 完成后必须执行）**

Step 6 所有分析输出完毕后，发送以下询问：

```
📊 分析报告已完成！

如需与其他项目进行宏观指标横向对比（整体满意度 / 续玩意愿 / NPS），
请把项目 bench 文档发给我，我会将对比数据整合进报告。

不需要的话无需回复，直接继续其他操作即可 🙂
```

**收到 bench 文档后的处理规则：**
- 读取 bench 文档中的整体满意度、续玩意愿、NPS 数据
- 与本次报告的宏观指标并排对比，格式如下：
```
          | 本项目  | Bench项目A | Bench项目B | ...
整体评分   | 2.4    | 2.1       | 1.8       |
NPS       | 51%    | 45%       | 38%       |
续玩意愿   | 2.1    | 1.8       | 1.5       |
```
- 对比结果追加在报告宏观指标区下方
- 不回复 → 默认不需要，直接进入后续步骤

---

### Step 7：交叉分析（按需执行）

触发条件：使用者提出交叉需求

**执行前二次确认：**
> "你说的第X题是'[题目文字]'，第Y题是'[题目文字]'，对吗？"

**支持类型：**
- 题目 × 题目（任意两题）
- 题目 × 留存（次日/三日/五日/七日/15日登录，仅自研平台）
- 题目 × 付费（LTV3/5/7/30/累计充值，仅自研平台）

**交叉分析排序规则：**

| 题目类型 | 排序方式 |
|---------|---------|
| 11点量表（-5~5） | 不倒序，按分值顺序 |
| 五分量表（1~5） | 不倒序，按分值顺序 |
| 年龄 | 不倒序，按年龄段顺序 |
| 性别 | 不倒序，按原始选项顺序 |
| 多选题 | 倒序（按选择率从高到低） |
| 亮点/痛点/期待等内容题 | 倒序（按占比从高到低） |
| 职业 | 倒序 |
| 其他题目 | 倒序 |

输出格式参考Gossip Harbor交叉表，详见 `references/crosstab-analysis.md`

---

### Step 8：项目记忆存储

**分析完成后，先检测运行环境，再选择对应存储方式：**

---

#### 环境检测（按顺序尝试）

```
1. 尝试调用 window.storage → 成功 → 走 Level 1（Claude.ai）
2. 尝试写入本地文件系统 → 成功 → 走 Level 2（Agent环境）
3. 两者均不可用 → 走 Level 3（纯对话环境，如飞书机器人）
```

---

#### Level 1：Claude.ai 环境（window.storage 可用）

完整持久化存储，跨会话记忆：

```javascript
await window.storage.set('project:{项目名}:{YYYY-MM}', JSON.stringify({
  project: 'W3',
  date: '2025-03',
  round: '三测',
  n_raw: 700,
  n_valid: 675,
  n_invalid: 25,
  core_metrics: {
    overall_score: 2.1,
    nps: 45.0,
    retention_intent: 1.8,
    module_scores: { '美术': 3.8, '玩法': 3.5 }
  },
  top_data: {
    highlights: [['玩法有趣', 0.68], ['美术好看', 0.52], ['题材吸引', 0.41]],
    painpoints: [['引导不清晰', 0.31], ['内容太少', 0.28], ['优化问题', 0.19]]
  },
  key_insights: '整体评分2.1分，NPS 45%...'
}));
```

存储成功后询问：
> "核心指标和Top数据已记住。是否保留完整题目数据，以便下次做任意题目历史对比？"

**历史唤起规则（Level 1）：**
- 识别项目名 → 读取 storage → 匹配历史记录 → 询问是否加入对比
- 只对题目文字一致或高度相似的题目做跨测次对比
- 自动标注显著变化（▲/▼ + 具体数字）

---

#### Level 2：Agent 环境（有本地文件系统，无 window.storage）

把历史数据写成本地 JSON 文件：

```python
import json, os

memory_dir = 'survey_memory'
os.makedirs(memory_dir, exist_ok=True)

record = {
  'project': 'W3',
  'date': '2025-03',
  'round': '三测',
  'n_raw': 700,
  'n_valid': 675,
  'n_invalid': 25,
  'core_metrics': {
    'overall_score': 2.1,
    'nps': 45.0,
    'retention_intent': 1.8,
    'module_scores': {'美术': 3.8, '玩法': 3.5}
  },
  'top_data': {
    'highlights': [['玩法有趣', 0.68], ['美术好看', 0.52]],
    'painpoints': [['引导不清晰', 0.31], ['内容太少', 0.28]]
  },
  'key_insights': '整体评分2.1分，NPS 45%...'
}

filename = f"{memory_dir}/W3_2025-03.json"
with open(filename, 'w', encoding='utf-8') as f:
    json.dump(record, f, ensure_ascii=False, indent=2)
```

存储后告知：
> "历史数据已保存为 `survey_memory/W3_2025-03.json`。
> 下次分析同一项目时，把这个 JSON 文件一起上传，我会自动恢复历史对比。"

**历史唤起规则（Level 2）：**
- 使用者上传 JSON 文件时，自动识别并读取历史数据
- 与本次数据合并，生成对比报告

---

#### Level 3：纯对话环境（飞书机器人等，无存储能力）

每次分析完成后，输出结构化文字存档，让使用者自己保存：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 【项目记忆存档】请复制保存到飞书文档或备忘录

项目：W3 ｜ 时间：2025-03 ｜ 轮次：三测 ｜ N=675
────────────────────────────
宏观指标
  整体评分：2.1（-5~5）
  NPS：45.0%
  续玩意愿：1.8（-5~5）
  美术评分：3.8（1~5）
  玩法评分：3.5（1~5）
────────────────────────────
Top亮点（前3）
  玩法有趣 68% ／ 美术好看 52% ／ 题材吸引 41%
Top痛点（前3）
  引导不清晰 31% ／ 内容太少 28% ／ 优化问题 19%
────────────────────────────
关键洞察
  整体评分2.1分，NPS 45%，整体处于良好水平...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 下次分析同一项目时，把以上存档内容发给我，
   我会自动识别并加入历史对比。
```

**历史唤起规则（Level 3）：**
- 使用者粘贴上次的存档文字时，自动识别"项目记忆存档"格式
- 解析其中的宏观指标和Top数据，纳入本次报告的历史对比列
- 告知使用者："已识别到 W3 三测（2025-03，N=675）的历史数据，将自动加入对比。"

---

## 参考文件索引

| 文件 | 内容 | 何时读取 |
|-----|-----|---------|
| `references/data-sources.md` | 四类数据源详细读取代码 | Step 1 遇到问题时 |
| `references/data-cleaning.md` | 各题型清洗与编码规则 | Step 3 处理数据时 |
| `references/question-types.md` | 各题型统计分析代码 | Step 4 补充参考时 |
| `references/output-template.md` | Excel报告输出模板 | Step 5 生成报告时 |
| `references/crosstab-analysis.md` | 交叉分析代码模板 | Step 7 执行交叉时 |
| `references/research-methods.md` | 主题分析/定量解读/用户画像/定性定量融合 | Step 6 AI分析时 |
